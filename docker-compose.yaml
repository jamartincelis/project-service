version: '3'

services:
    project-service:
        container_name: project-service
        build: .
        command: >
            sh -c "python src/manage.py runserver 0.0.0.0:8000"
        volumes:
            - .:/code
        env_file:
            - '.env'